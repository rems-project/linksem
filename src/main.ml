(*Generated by Lem from main.lem.*)
open Lem_function
open Lem_string
open Lem_tuple

open Bitstring_local
open Default_printing
open Error
open Missing_pervasives
open Show

open Elf_header
open Elf_file1
open Elf_executable_file2
open Elf_executable_file3
open Elf_linking_file2
open Elf_linking_file3
open Elf_linking_file4

open Sail_interface

let default_hdr_bdl =
  (default_os_specific_print, default_proc_specific_print)

let default_pht_bdl =
  (default_os_specific_print, default_proc_specific_print)

let default_sht_bdl =
  (default_os_specific_print, default_proc_specific_print, default_user_specific_print)

let _ =  
(let res =    
(Ml_bindings.acquire_bitstring "/usr/bin/ld" >>= (fun bs0 ->
    Elf_executable_file3.read_elf32_executable_file3 bs0 >>= (fun f1 ->
    let _ = (print_endline (Elf_executable_file3.string_of_elf32_executable_file3 default_hdr_bdl default_pht_bdl default_sht_bdl f1)) in
    Elf_executable_file3.get_elf32_dynamic_symbol_table f1 >>= (fun symtab ->
    let _ = (print_endline ("Symbol table length: " ^ string_of_int (List.length symtab))) in
    let _ = (print_endline (Elf_symbol_table.string_of_elf32_symbol_table symtab)) in
    Elf_executable_file3.get_elf32_string_table f1 >>= (fun strtab ->
    Elf_symbol_table.get_elf32_symbol_image_address symtab strtab >>= (fun strs ->
    let _ = (print_endline ("Length: " ^ (string_of_int (List.length strs)))) in
    return f1))))))
  in
    (match res with
      | Fail err -> print_endline ("[!]: " ^ err)
      | Success _ -> print_endline "Success..."
    ))